[package]
name = "comms-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Wire types for citrate-comms — Envelope, GroupId, Commit/Welcome/AppMsg, RoleAssertion, AuditRecord. No crypto, no I/O."

[dependencies]
# Wire types only — serde + CBOR + zeroize. Deliberately no crypto or socket deps;
# this crate is shared by relay, core, and client so all three agree on byte layout.
